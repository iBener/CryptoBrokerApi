version: '3.4'

services:
  apigateway:
    image: ${DOCKER_REGISTRY-}apigateway
    build:
      context: .
      dockerfile: Presentation/ApiGateway/Dockerfile

  crypto.brokerapi:
    image: ${DOCKER_REGISTRY-}cryptobrokerapi
    build:
      context: .
      dockerfile: Crypto.BrokerApi/Dockerfile

  crypto.ordersapi:
    image: ${DOCKER_REGISTRY-}cryptoordersapi
    build:
      context: .
      dockerfile: Crypto.OrdersApi/Dockerfile

  notify.emailapi:
    image: ${DOCKER_REGISTRY-}notifyemailapi
    build:
      context: .
      dockerfile: Notify.EmailApi/Dockerfile

  notify.pushapi:
    image: ${DOCKER_REGISTRY-}notifypushapi
    build:
      context: .
      dockerfile: Notify.PushApi/Dockerfile

  notify.smsapi:
    image: ${DOCKER_REGISTRY-}notifysmsapi
    build:
      context: .
      dockerfile: Notify.SmsApi/Dockerfile
